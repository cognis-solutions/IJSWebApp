import { IjsAutoScrollDirective } from './ijs-auto-scroll.directive';

describe('IjsAutoScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new IjsAutoScrollDirective();
    expect(directive).toBeTruthy();
  });
});
